STRAPI_API_TOKEN=your-read-only-api-token
STRAPI_URL=http://localhost:1337

# Preview mode (must match backend PREVIEW_SECRET)
# The API token must have "find" permission for draft documents in Strapi
PREVIEW_SECRET=your-preview-secret
