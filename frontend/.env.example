# Strapi backend URL
# Development: http://localhost:1337 (default, no need to set)
# Netlify build: set this to a tunnel URL or leave unset to skip CMS pages
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
